#include <bits/stdc++.h>
#define ll long long int
const ll MOD = 1e9 + 7;
using namespace std;

void solve() {
    int n;cin >> n;
    char ch;cin >> ch;
    string s;cin >> s;
    set<char> st;

    for(char c:s) {
        st.insert(c);
    }

    
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    ll t;
    cin >> t;
    while (t--) {
        solve();
    }
}