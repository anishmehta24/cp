#include <bits/stdc++.h>
using namespace std;
#define int long long


void solve() {
    int n;
    cin >> n;
    int x;
    cout << "mul 9\n";
    cout.flush();
    cin >> x;
    cout << "digit\n";
    cout.flush();
    cin >> x;
    cout <<"digit\n" ;
    cout.flush();
    cin >> x;
    cout << "add " << n-9 << "\n";
    cout.flush();
    cin >> x;
    cout << "!" <<"\n";
    cout.flush();
    cin >> x;

}

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    int t;
    cin >> t;
    while (t--) {
        solve();
    }
    return 0;
}
